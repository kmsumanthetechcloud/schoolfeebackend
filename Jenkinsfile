pipeline {
	agent any 
	stages {
		stage ('checkout') {
			steps {
				echo 'code checkout successful'
				}
			}
			stage9('workspace'){
				steps {
				sh 'pwd'
				sh 'ls -la'
				}
			}
			
		}
	}
